# ポケモン一括ダメージ計算ツール 取扱説明書

## はじめに

このツールは、**ポケモン対戦（主にダブルバトル）の育成論作成**を補助するWebアプリです。
自分のポケモンが、メタ上位の相手たちに対して何発耐えるか・何発で倒せるかを**一括で計算**できます。また、特定の条件（確定1発/2発耐え等）を満たすための**最小努力値（EV）**を自動で探索する機能も備えています。

---

## 画面の構成

画面は大きく3つのエリアに分かれています。

```
┌─────────────────────────────────────────┐
│          ポケモン一括ダメージ計算           │
├─────────────────────────────────────────┤
│  コントロールパネル                        │
│  [自分のポケモン] [相手リスト] [チームの編集] │ ← タブで切り替え
│                                          │
│  （選択したタブの内容）                    │
├─────────────────────────────────────────┤
│  [計算] ボタン                            │
├─────────────────────────────────────────┤
│  結果タブ                                 │
│  [攻撃] [防御] [防御ライン] [攻撃ライン]    │
│  （計算結果テーブル）                      │
└─────────────────────────────────────────┘
```

---

## ステップ1：自分のポケモンを設定する

コントロールパネル上部の **「自分のポケモン」** タブを選択します（ピンク色のタブ）。

### ポケモンを選ぶ

**種族名の入力欄**に、ポケモン名を入力します。

- **日本語**でも**英語**でも入力できます
- 入力すると候補リストが表示されるので、クリックして選択します
- 選択すると、プリセットの努力値・特性・性格が自動的にセットされます
- プリセットが用意されているポケモンは設定が読み込まれ、ない場合は無振り・無補正の初期値になります

> **例**: 「ガオガエン」と入力 → 候補の「ガオガエン」をクリック → いかく・ゆうかん・努力値が自動セットされる

---

### 基本情報を設定する

| 設定項目 | 説明 |
|----------|------|
| **持ち物** | リストから選択するか、名前を直接入力します |
| **特性** | そのポケモンの特性一覧から選択します |
| **性格** | 25種類の性格から選択します。上昇・下降するステータスがピンク/水色で色分け表示されます |
| **テラスタイプ** | テラスタル後のタイプを選択します（ステラ含む全19タイプ） |

---

### 努力値（EV）を設定する

各ステータス（HP・攻撃・防御・特攻・特防・素早さ）ごとに設定します。

#### 設定方法は2つ

1. **スライダーを動かす**: 0〜252の範囲で4の倍数刻みで調整
2. **数値を直接入力**: テキストボックスに数値を入力してEnterキーまたはフォーカスアウトで確定

> スライダーの隣に**実数値**がリアルタイムで表示されます（性格補正込み）

#### 制約

- 1つのステータスの最大は**252**
- 全ステータスの合計最大は**510**（510を超えて振ることはできません）
- 入力値は自動的に4の倍数に丸められます

#### 個体値（IV）

努力値の右側のボックスで個体値を設定できます（0〜31）。
通常は全て31のままでOKです。素早さを操作したい場合（最遅など）は0に変更します。

---

### ランク変化を設定する

ステータスの下にランク変化（積み技などによる上昇/下降）を設定できます。

- **+6 〜 −6** の範囲でプルダウンから選択
- 攻撃側・防御側両方に設定できます

> 例：剣の舞1回使用後の計算 → 攻撃のランク変化を **+2** に設定

---

### 技を設定する

技スロットは4つあります。それぞれオートコンプリートで入力できます。

- そのポケモンが覚えられる技が候補に表示されます
- 日本語・英語どちらでも検索可能
- 空欄のスロットは計算の際に無視されます

> **注意**: プリセット読み込み後、技は空欄になります。必ず手動で設定してください。

---

## ステップ2：相手チームを確認・管理する

コントロールパネルの **「相手リスト」** タブ（緑色）を選択します。

### デフォルトチーム「伝説レギュ上位」

初期状態では、環境上位のメタポケモンたちがセットされています。
このチームは**削除できませんが**、除外チェックで計算対象から外すことができます。

---

### 除外チェックボックス

各ポケモンの横にあるチェックボックスをオフにすると、そのポケモンを**計算から除外**できます。

> 使わない相手や、とりあえず特定の相手だけで試したい場合に便利です

---

### チームを切り替える

「相手チーム:」のドロップダウンから、別のボックスを選択できます。
最初は「伝説レギュ上位（デフォルト）」のみですが、カスタムチームを作成できます。

---

### チームの操作ボタン

| ボタン | 説明 |
|--------|------|
| **+ 新規チーム** | 新しいカスタムチームを作成します |
| **名前変更** | 現在のチームの名前を変更します（デフォルト不可） |
| **削除** | 現在のチームを削除します（デフォルト不可） |
| **複製** | 現在のチームをコピーして新しいチームを作成します |

---

### ポケモンを追加する（「ポケモンを追加」セクション）

#### 手動で追加

**「+ 新規ポケモン追加」** ボタンをクリックすると、「チームの編集」タブに移動します。
種族名を入力してポケモンを設定し、「このボックスに追加」ボタンで保存します。

#### PokePasteインポート

**「PokePaste インポート」** ボタンをクリックすると、テキスト入力欄が開きます。
Smogon形式のPokePasteテキストを貼り付けて「インポート」ボタンを押すと、チームにポケモンが追加されます。

```
【PokePaste形式の例】
Incineroar @ Safety Goggles
Ability: Intimidate
Level: 50
EVs: 252 HP / 164 Atk / 60 Def / 28 SpD / 4 Spe
Brave Nature
IVs: 0 Spe
- Fake Out
- Flare Blitz
- Close Combat
- Parting Shot
```

> 日本語のポケペ形式でも入力できます

#### PokePasteエクスポート

**「PokePaste エクスポート（日）」** または **「PokePaste エクスポート（英）」** で、
現在のチームをPokePaste形式のテキストとしてコピーできます。

---

## ステップ3：チームを編集する（チームの編集タブ）

コントロールパネルの **「チームの編集」** タブ（紫色）では、相手ポケモンの細かい設定ができます。

### 相手ポケモンの読み込み方法

1. **種族名を入力**: 検索フィールドに入力してリストから選択
2. **メタリストから読み込み**: 相手リストのポケモン名をクリックして編集画面を開く

### 編集できる項目

自分のポケモンと同様に、以下を編集できます：
- 持ち物・特性・性格・テラスタイプ
- 努力値・個体値・ランク変化
- 技（4スロット）
- カスタムラベル（備考として表示される名前）

### 保存方法

| ボタン | 説明 |
|--------|------|
| **このボックスに追加** | 新しいポケモンとして現在のボックスに追加 |
| **変更を保存** | 既存のポケモンの設定を上書き更新 |

> 特性がActive/Inactiveバリアントを持つ場合（例：いかく）、追加時に複数のバリアントが自動生成されます

---

## ステップ4：計算を実行する

全ての設定が完了したら、**「計算」ボタン**（大きなボタン）をクリックします。

計算中は「計算中...」と表示されます。相手の数が多いほど時間がかかります。

---

## 結果を見る

計算が完了すると、4つのタブに結果が表示されます。

---

### 攻撃タブ

**自分のポケモン → 各相手** へのダメージを表示します。

| カラム | 説明 |
|--------|------|
| 相手 | 対戦相手のポケモン（アイコン付き）。クリックで詳細表示 |
| 技 | 使用した技名 |
| ダメージ | ダメージ割合の範囲（例: 60.5% ～ 71.3%） |
| 確定数 | 「確定1発」「乱数1発(87.5%)」「確定2発」など |
| 相手HP | 相手のHP実数値 |
| 相手防御実数 | 相手の防御/特防実数値 |
| 自分実数値 | 自分の攻撃/特攻実数値 |
| 自分持ち物 | 自分の持ち物（アイコン） |
| 自分テラスタル | テラスタイプ（アイコン） |
| 場の状態 | 計算時のフィールド状態 |

> 行をクリックすると詳細ポップアップが開きます

---

### 防御タブ

**各相手 → 自分のポケモン** へのダメージを表示します。

物理技と特殊技が別々に表示されます。

| カラム | 説明 |
|--------|------|
| 相手 | 攻撃してくる相手ポケモン |
| 技 | 相手の使用技 |
| ダメージ | 受けるダメージ割合 |
| 確定数 | 何発で倒されるか |
| 相手攻撃実数 | 相手の攻撃/特攻実数値 |
| HP実数 | 自分のHP実数値 |
| 防御/特防実数 | 自分の防御/特防実数値 |

---

### 防御ラインタブ

**「○発耐えるために必要な最小努力値」** を表示します。

現在設定している努力値ではなく、「何振りあれば耐えられるか」を自動探索した結果です。

| カラム | 説明 |
|--------|------|
| 必要実数値 | 必要なHP/防御の実数値 |
| 相手 | 攻撃してくる相手 |
| 技 | 相手の技 |
| 必要努力値 | 必要な努力値（例: H252B4, H196 など） |
| 自分テラスタル | テラスタル状態 |
| 自分持ち物 | 持ち物 |
| 特化 | B/D特化した場合の耐え結果 |
| 無補正252 | 無補正252振りの場合の耐え結果 |
| 場の状態 | フィールド状態 |

**ソート**: 「必要努力値 少ない順」「多い順」でソートできます

---

### 攻撃ラインタブ

**「○発で倒すために必要な最小努力値」** を表示します。

現在の努力値で倒せるかどうかだけでなく、「何振りあれば確定○発になるか」を示します。

| カラム | 説明 |
|--------|------|
| 必要実数値 | 必要な攻撃/特攻の実数値 |
| 相手 | ターゲットの相手 |
| 技 | 使用技 |
| 必要努力値 | 必要な努力値（例: C252+ など） |
| 自分テラスタル | テラスタル状態 |
| 自分持ち物 | 持ち物 |
| 相手HP | 相手のHP実数値 |
| 相手耐久 | 相手の耐久実数値 |
| H4 | H4振り想定の計算結果 |
| H252 | H252振り想定の計算結果 |
| HB/HD特化 | HB or HD特化想定の計算結果 |

**ソート**: 「必要努力値 少ない順」「多い順」でソートできます

---

### 行クリックで詳細表示

テーブルの行（特に「相手」の列）をクリックすると、詳細ポップアップが開きます。

ポップアップには以下が表示されます：
- 相手ポケモンのアイコン・テラスタイプ・持ち物
- 計算に使用した全パラメータ（ダメージ、実数値など）

`Esc`キーまたは背景クリックでポップアップを閉じます。

---

### 結果の多ページ表示

結果が多い場合は最初の50件のみ表示されます。

- **「Show More」**: 50件ずつ追加表示
- **「Show All」**: 全件表示

---

## フィールド設定（詳細）

> 現時点ではコード内に設定項目があります。UIからの設定方法は今後のアップデートで追加される予定です。

計算時のフィールド状態として以下が考慮されます：

| 設定 | 内容 |
|------|------|
| 天候 | なし / 日照り / 雨 / 砂嵐 / 雪 |
| フィールド | なし / エレキ / グラス / サイコ / ミスト |
| 自分側 | リフレクター / ひかりのかべ / フレンドガード / てだすけ |
| 相手側 | リフレクター / ひかりのかべ / フレンドガード |
| その他 | 剣のほこら / 玉の壺 / ひのきのぼ / がんしゃく / スプレッドダメージ |

**スプレッドダメージ**: ONにするとダブルバトルの複数対象技のダメージが0.75倍になります（デフォルトON）。

---

## バリアント（Active/Inactive）について

天候や状態異常によって効果が変わるポケモンは、**Active** と **Inactive** の2つのバリアントとして計算されます。

| バリアント | 意味 |
|------------|------|
| **(Active)** | 特性が発動している状態（例: 日照り中のひでり） |
| **(Inactive)** | 特性が発動していない状態 |

この表示は、同じポケモンの「天候あり/なし」や「状態異常あり/なし」の比較に使います。

---

## よくある使い方

### 育成論を作る場合

1. 自分のポケモンの種族・持ち物・特性・性格を設定
2. 努力値を仮設定（例: H252 B4 S252 など）
3. 計算を実行して防御タブを確認
4. 「防御ラインタブ」で「この相手の技を耐えるのに何振り必要か」を確認
5. 努力値を調整してもう一度計算

### 特定の相手だけ確認したい場合

1. 相手リストタブで関係ない相手のチェックをオフにする
2. または新しいチームを作って必要な相手だけ追加する

### 他の人のレンタルチームを分析する場合

1. 「チームの編集」タブで相手のポケモンを1体ずつ追加
2. または「PokePaste インポート」で一括入力
3. 自分のポケモンに対してどれだけ有利か確認

---

## 注意事項

- 計算はダブルバトル（レベル50）を前提としています
- スプレッドダメージONの場合、複数対象技のダメージは0.75倍になります
- テラスタル後の計算では、自分/相手それぞれのテラスタイプ設定が使われます
- 「伝説レギュ上位」チームはアプリ更新時に最新のメタリストで自動更新されます
- データはブラウザのlocalStorageに自動保存されるため、ページを閉じても設定が残ります

---

## データの保存について

チーム（ボックス）の設定はブラウザのlocalStorageに自動保存されます。

- **自動保存**: 変更があるたびに即時保存
- **同期なし**: データはブラウザ内のみに保存されます（他の端末では共有されません）
- **バックアップ**: 「PokePaste エクスポート」でデータを外部にコピーしておくことをお勧めします

---

*このツールはSmogonの公式ダメージ計算エンジン [@smogon/calc](https://github.com/smogon/damage-calc) を使用しています。*
